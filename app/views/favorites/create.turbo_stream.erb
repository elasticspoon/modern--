<%= turbo_stream.append "favorite-concerts-list", @favorite %>
<%= turbo_stream.replace dom_id(@favorite.concert), @favorite.concert %>
<% if current_user.favorites.any? %>
  <%= turbo_stream.replace "no-favorites", "" %>
<% end %>
